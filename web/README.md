# 前端 · I01

Next.js 16.3.5 / React 19.3.0 / TypeScript 5.9.3 / Tailwind 4.3.3，pnpm-lock.yaml 固定依赖。安装、启动和验证见 [开发指南](../docs/development.md)。

首页三入口共用页面内草稿，切换入口清除预览摘要，刷新清空全部草稿。当前不保存会话、不提供推荐；读取真实基础状态，展示未准备好与故障重试。浏览器没有数据库/模型密钥，不内置真实商品或伪造推荐。

lib/api/schema.d.ts 由后端 OpenAPI 生成，用 `pnpm run api:generate` 同步，不能手改。Next 只桥接固定状态 GET，API_BASE_URL 为服务端配置，不接受浏览器输入的 URL。

类型、格式、构建和 Playwright 命令已可用。E2E 需迁移后的数据库，使用真实 API，覆盖 375/768/1440 三个宽度的入口、草稿、空状态、断网重试和键盘操作；不等于全面辅助技术审计。

目录/详情、PC 清单/笔记本卡片、比较/导出、对话与后台按后续阶段加入，P6 整合完整体验。
