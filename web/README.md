# 前端边界（待实现）

计划 Next.js、React、TypeScript、Tailwind、pnpm。P1 核验支持范围和互相兼容版本，再创建 package.json、pnpm-lock.yaml 与应用。不在本轮放置无法启动的依赖清单。

页面按阶段增加：首页三个入口→需求表单→目录与详情→PC 清单/笔记本卡片→比较/导出→对话→管理后台。状态包括加载、空、失败、过期、证据不足。

浏览器不持有模型/来源密钥；不硬编码真实商品。中文预算与范围始终可见；375/768/1440 视口和键盘操作纳入 P6 E2E。对话不能遮住商品操作，模型不可用时保留确定性流程。
