# 项目进度

更新日期：2026-09-21。当前交付：步骤 05 / T02。分支：codex/t02-evidence-data-model；起点：f7dc2b2。最终提交以 git log -1 为准。

## 本轮成果与边界

- 用户确认 I02 后继续一个步骤；额度中断后续做同一 T02，未跳到 T03。
- 新增 13 张商品/来源/文档/证据/事实/商家/listing/报价领域表，以及 Pydantic 记录契约。金额整数分、未知为 null；复合外键保护证据 SKU 范围、类型/单位/类别、地区与 synthetic 隔离。
- 精确身份部分唯一索引处理空 revision，未知身份留待确认；事实允许保留冲突；来源权限默认 unknown，审核不等于发布。
- 自包含 0003_catalog 迁移、ready 版本同步、OpenAPI 与前端类型同步。现有会话/画像流程保持可用，平台仍显示数据准备中。
- 没有导入/发布/目录查询接口、真实商品/报价、Provider、总价函数、推荐或模型调用。配置指纹生成与审核发布门禁在 T03；快照追加写服务和时效策略在 T05。
- [模型与关系图](catalog-model.md)、[验收](research/T02-acceptance.md)、[PR 说明](reviews/T02-PR.md) 可审查；PROJECT_SPEC 原文保留。

## 验证记录

- 本机真实 PostgreSQL：94 项后端通过，无跳过。覆盖 I02 会话回归，以及升级/重复升级/降级/重建保留画像、有效证据链、重复精确身份、地区/配置差异、错 SKU 证据、无来源、单位/类别、缺失值、费用、时间和合成隔离。第三方测试客户端两项弃用警告保留。
- 真实 API 浏览器 15 项通过，375/768/1440；本轮未改变界面布局。测试使用独立 test_* 库和 3001/8001，未复用用户预览。商品测试记录均 synthetic=true、TEST-*。
- ruff 检查/格式、前端格式/类型/生产构建、OpenAPI/类型生成通过。pip-audit 与 pnpm audit 未检出已知漏洞。
- 本机 Redis 显式关闭；完整 PG/Redis 的本轮 CI 结果待推送后记录，不以旧轮 CI 代替。

## Git 与接续

本轮基于 I02 已确认内容，保护中断前已有改动。#1—#4 仍 OPEN、未合并；T02 的 PR base 为 codex/i02-sessions-profiles，只审查第 5 步增量。未合并、未生产部署。

## 下一轮（必须等待确认）

步骤 06 / T03：人工导入、预览、审核与事务发布。D01 使用许可、D02 精确身份/BIOS、D03 真实报价仍未解除；不以合成数据抵消真实数据验收。本轮完成后停止，不自动进入 T03。
