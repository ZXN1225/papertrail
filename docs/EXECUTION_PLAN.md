# 完整执行计划

## 本轮范围与推进方式

以用户明确要求为边界：第一步是规划和基础文件，不执行附件 §19.4 示例提示词中的整个 P0/P1。当前已推进至步骤 03 / I01，实际结果见 PROGRESS.md。阶段编号 P0—P7 保留原规格定义；下面的“步骤”是便于逐次确认的交付单元，一个阶段可以分多轮。

每轮固定流程：读取进度 → 写本轮验收 → 实现 → 运行相关检查 → 更新台账与 PR → 汇报结果与限制 → 等待用户确认。未完成的外部条件保持未完成，不能用模拟数据抵消真实数据验收。

默认范围：CN/CNY、中文、全新零售；PC、笔记本、单零件三入口；匿名会话。MVP 不做下单、二手件、复杂水冷、账号长期记忆、性能预测和多 Agent。

## 分步交付与验收

| 步骤 / 阶段 | 实施范围 | 可展示成果 | 必须验收 / 前置 |
|---|---|---|---|
| 01 / P0 起步 | T01：规格归档、执行计划、台账、ADR、模板、模块边界 | 可审查的仓库与检查脚本 | 附件副本一致；台账覆盖 T01—T16；无虚构商品与密钥；本轮停止 |
| 02 / P0 验证 | V01：CPU、主板、笔记本、报价四类来源核验；核心语义细化 | 来源权限表、字段覆盖、失败记录；技术版本矩阵 | 每源至少 5 个精确样本或可定位失败证据；无价格权限明确采用真实人工报价；官方支持与兼容性核验后记录运行时和依赖候选 |
| 03 / P1 基础运行 | I01：FastAPI、Next.js、配置、PG/Redis 开发环境、首次基础迁移、CI | 浏览器首页三入口和空状态；live/ready；全新环境启动指南 | 提交 uv.lock/pnpm-lock.yaml；锁定安装、类型、构建、空库迁移通过；数据库断开 ready=503；生产缺关键配置启动失败 |
| 04 / P1 会话 | I02 + T11：匿名身份、画像 revision、资源隔离 | 保存/修改画像；明确缺数据状态 | HttpOnly/SameSite、生产 Secure、CSRF/Origin；跨会话统一 404；并发 revision=409；新对话隔离；不接模型 |
| 05 / P2 数据模型 | T02：SKU、事实、证据、来源、报价关系与迁移 | 数据字典、OpenAPI、PG 模型 | 精确身份唯一、外键、非负金额、有效时间；空库迁移与升级路径；不得以 SQLite 替代 |
| 06 / P2 导入发布 | T03：人工导入、dry-run、待审、事务发布、任务/outbox | 错误行和差异预览；一组真实样本端到端 | 重复导入幂等；坏批不污染发布版本；拒绝生产 synthetic；许可未确认不发布 |
| 07 / P2 目录报价 | T04/T05：目录 API、只读页面、ManualOfferProvider、总价快照 | 有来源的 SKU 详情、报价时间与缺价状态 | 精确 SKU 同口径；分精度、税/运费/资格、TTL、null；至少一个可靠规格适配器闭环 |
| 08 / P3 基础规则 | T06：C001—C003 | CPU/主板/内存规则报告 | 同 socket 不等于 BIOS 通过；BIOS 未知阻断；内存代际、容量和套装反例 |
| 09 / P3 完整规则 | T07：C004—C012、完整性聚合 | 尺寸、供电、SSD、输出、硬需求报告 | 未执行/未知不能 validated；连接器、布局、端口共享、自带散热证据和数量边界 |
| 10 / P4 笔记本 | T08：硬过滤、版本化评分、稳定排序 | 无 LLM 笔记本推荐、差异说明 | 同系列不同屏幕/内存不串 SKU；缺失分数区间；未知硬需求不能满足 |
| 11 / P4 PC | T09：有界组合搜索、剪枝、锁定/已有件 | 无 LLM PC 清单与可行性报告 | 小集合对照穷举；预算多 1 分拒绝；超时与无解区分；不宣称全市场最优 |
| 12 / P4 快照 | T10：保存、修订、比较、Markdown/JSON 导出 | 可保存和重新核价的两条确定性流程 | 历史不可覆盖；改件重验整机；幂等键冲突 409；所有者隔离；过期不计当前预算 |
| 13 / P5 Agent | T12：Provider、工具 Schema、状态机、有界循环 | 需求追问与受控工具运行 | 4 回合/8 调用/45 秒初始限额，重试计入；参数/权限、去重；模型失败回退；硬约束不丢失 |
| 14 / P5 知识和输出 | T13/T14：审核手册、BM25、引用、回答契约、SSE/cancel | 可验证引用、多轮修订与取消 | 事实校验后展示；错 SKU 引用拒绝；注入不提权；断线恢复；旧 revision 不覆盖新结果 |
| 15 / P6 产品 | T15：问卷、清单编辑、对比、数据后台、可访问性 | 完整用户与管理员流程 | 375/768/1440 视口两条 E2E；键盘焦点；加载/空/错误/过期状态；真实 API 驱动 |
| 16 / P7 验收 | T16-A：金标、故障注入、压测、隐私与安全复核 | 可复现评估报告和限制清单 | §16 正确性门槛；120 场景及独立保留集；LLM/Redis/worker 故障；报告性能实测，不伪造指标 |
| 17 / P7 交付 | T16-B：部署、迁移、备份恢复、回滚、试运行 | 指定环境的可访问版本和运维文档 | 前置域名/服务器/授权；隔离恢复演练；ready、两条冒烟、不可变镜像；展示范围与权限；部署按当轮授权 |

## 依赖与工作顺序

主线 P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7。T11 在 P1 做基础状态，P5 扩充 Agent 相关状态。T08 与 T09 都依赖报价，T09 另依赖完整兼容规则；T10 等待两条推荐链。页面随 API 逐步搭建，P6 负责整合体验，不能到 P6 才验证所有接口。T13 依赖已审核导入；没有文档许可则保持知识缺失，不使用未审核内容。

## 架构和技术决策落地

- 浏览器 → FastAPI → 共享领域服务 → PostgreSQL；Agent 只能通过受控工具使用同一服务。
- Redis 用于缓存/限流；采集、索引和长运行任务在 worker；任务租约与 outbox 防止半发布。
- 使用 Python 3.12、FastAPI/Pydantic v2、SQLAlchemy 2/Alembic、Next.js/React/TypeScript/Tailwind、pnpm/uv。I01 已安装、锁定并验证基础组合，精确版本与环境边界见 runtime-matrix.md。
- 步骤 02 查询官方支持信息，步骤 03 实际锁定 Python/Node/pnpm/uv、全部依赖和容器版本。提交锁文件；CI 用锁定安装。没有锁文件前不宣称可复现应用构建。
- BM25 先行；pgvector、重排与多 Agent 均需后续对照评估，MVP 不提前加入。

## 验证与发布门禁

每个任务执行受影响的单元、契约、PG 集成和 UI 测试，不用与实现同源的答案代替独立金标。固定时钟、快照、随机种子。CI 不默认调用付费模型；模型/提示词/工具变更另附离线评估。

MVP 固定验收集：已展示正式候选硬约束、预算、关键事实追溯、缺失诚实、报价新鲜均 100%；兼容误放率 0。任务完成率 ≥90%，路由 ≥95%，RAG coverage@5 ≥85%，双侧证据覆盖 ≥90%，同时报告分母、覆盖范围与限制。所有数字是目标，当前无实测成绩。

初始数据目标：20—40 笔记本 SKU、60—100 零件 SKU、至少 10 套规则通过组合；不能为凑数量伪造。真实数据不足时单列“功能演示通过 / 真实数据未通过”。

## 风险与需要决定的节点

| 风险 / 待决项 | 处理与决定时机 |
|---|---|
| 报价 API 未授权、字段不全 | 步骤 02 确认人工维护路径；步骤 06/07 导入真实证据和有效期 |
| SKU 身份、BIOS、尺寸证据缺失 | 待审/unknown；先闭合少量完整组合，不能放宽规则 |
| 本机缺 Python 3.12 / Docker | 步骤 03 安装或选择用户可用 PG 环境；不偷换 SQLite |
| 模型账号和成本 | 步骤 13 前选择 Provider 和预算；未配置继续确定性模式 |
| GitHub 访问、账号与代码许可 | 单独记录网络/认证状态；许可未定不复制第三方代码 |
| 域名、服务器、数据展示权限 | 步骤 17 前落实；无账号只交付部署配置，不声称已上线 |

附件估计 21—35 人日，数据整理和授权等待另计；这是规划估计，不是交付日期。用户每轮确认的节奏优先于该估算。V1 在稳定数据/实际需求后加入授权刷新、账户、pgvector；V2 只有同预算基线证明收益后加入多 Agent、重排、预测或学习。
